# M4 Admin Implementation Plan

> **For agentic workers:** Execute task-by-task. Steps use checkbox syntax.

**Goal:** Staff admin for catalog CRUD and order operations on local Supabase.

**Architecture:** Signed admin cookie + service-role catalog writes + `admin_update_order` RPC for stock-safe order transitions.

**Tech Stack:** Next.js App Router, Zod, Supabase JS service role, PostgreSQL, Vitest.

## File map

- Create: `lib/admin/*`, `app/admin/**`, `components/admin/**`, `tests/admin/**`
- Create: `supabase/migrations/202607250009_admin_order_ops.sql`, `supabase/tests/admin.sql`
- Modify: `.env.example`, `README.md`

## Tasks

### Task 1 — Auth + service role + env
### Task 2 — Order RPC + SQL tests
### Task 3 — Status rules + validation + unit tests
### Task 4 — Queries + product/order actions
### Task 5 — Admin UI pages
### Task 6 — Quality gate

Constraints: new migration only; no production; no service-role in client; do not edit applied migrations.
